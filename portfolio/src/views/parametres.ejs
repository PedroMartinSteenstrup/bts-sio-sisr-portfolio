<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partials/head'); %>
</head>
<header>
    <%- include('partials/header'); %>
</header>

<body>
    <main class="main-content">
        <p>Bienvenue, <%= user %>!</p>
        <p>Vous êtes connecté(e) jusqu'à <%= exp.getHours() +`h`+ exp.getMinutes() %>.</p>
        <p>Cela vous donne accès à</p>
        <ul>
            <li><a href="/upload">La possibilité de téléverser des fichiers</a></li>
            <li><a href="/test" target="_blank">Accès à la page de tests</a></li>
        </ul>
    <p>
    <% if (user) { %>
          <form action="/logout" method="POST" style="display:inline;">
            <button class="nav-link" type="submit" style="background:none;border:none;padding:0;color:inherit;cursor:pointer;">Logout</button>
          </form>
      <% } else { %>
          <a class="nav-link" href="/login">Connexion</a>
      <% } %>
    </p>
<br>
<p>
    
</p>

</main>
</body>
<footer>
    <%- include('partials/footer'); %>
</footer>
</html>